

export default function Professional({handleClick, handleTwo}){
  return(
    <div>
      <button onClick={handleClick}>Left Bottom Square</button>
      <button onClick={handleTwo}>Right Bottom Square</button>
    </div>
  )
}